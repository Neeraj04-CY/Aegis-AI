[build-system]
requires = ["setuptools>=67", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis"
version = "0.0.0"
description = "AEGIS — Autonomous Executive Governance & Intelligence System (Phase 0)"
authors = [{ name = "AEGIS Team" }]
requires-python = ">=3.10"
dependencies = []